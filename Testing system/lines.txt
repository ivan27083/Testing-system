№1 Что такое строка в языке C++?
1)Это структура данных;
2)Это массив символов, завершающийся нулевым символом;*
3)Это указатель на массив символов;
4)Это переменная типа string;

№2 Как объявить строку в C++?
1)char s;
2)string s;
3)char[] s;
4)char* s;*

№3 Какой символ используется для обозначения конца строки в C++?
1)\n;
2)\r'
3)\0;*
4)\;

№4 Какой заголовочный файл нужно подключить для работы со строками в C++?
1)iostream;
2)string.h;*
3)cstring;
4)stdio.h;

№5 Какой оператор используется для сравнения строк в C++?
1)==;
2)!=;
3)<;
4)все вышеперечисленные;*

№6 Какой метод класса string используется для получения длины строки?
1)length;*
2)size;
3)capacity;
4)max_size;

№7 Как добавить одну строку к другой в C++?
1)s1 + s2;
2)s1.append(s2);*
3)s1.concat(s2);
4)s1.add(s2);
 
№8 Какой метод класса string используется для получения подстроки из строки?
1)substr;*
2)slice;
3)substring;
4)subarray;

№9 Как получить символ строки по индексу в C++?
1)s[index];
2)s.get(index);
3)s.at(index);*
4)все вышеперечисленные

№10 Какой метод класса string используется для замены символов в строке?
1)replace;*
2)swap;
3)insert;
4)remove;

№11 Какой метод класса string используется для поиска подстроки в строке?
1)find;*
2)search;
3)locate;
4)seek;

№12 Как удалить символ из строки в C++?
1)s.delete(index);
2)s.erase(index);
3)s.remove(index);
4)все вышеперечисленные;*

№13 Как добавить символ в конец строки в C++?
1)s.append(c);
2)s.add(c);
3)s.push_back(c);*
4)s.insert(c);

№14 Как получить часть строки до первого пробела?
1)s.substr(0, s.find(' '));
2)s.substr(0, s.find_first_of(' '));*
3)s.substr(0, s.find_last_of(' '));
4)s.substr(0, s.find_first_not_of(' '));

№15 Как получить часть строки после первого пробела?
1)s.substr(s.find(' ') + 1);
2)s.substr(s.find_first_of(' ') + 1);*
3)s.substr(s.find_last_of(' ') + 1);
4)s.substr(s.find_first_not_of(' ') + 1);

№16 Как проверить, начинается ли строка с определенного символа?
1)s[0] == c;
2)s.at(0) == c;
3)s.front() == c;
4)все вышеперечисленные;*

№17 Как проверить, заканчивается ли строка на определенный символ?
1)s.back() == c;
2)s.at(s.length() - 1) == c;
3)s[s.length() - 1] == c;
4)все вышеперечисленные;*
 
№18 Какой метод класса string используется для преобразования строки в верхний регистр?
1)toUpper;
2)uppercase;
3)transform;*
4)upperCase;

№19 Какой метод класса string используется для преобразования строки в нижний регистр?
1)toLower;
2)lowercase;
3)transform;*
4)lowerCase;

№20 Какой метод класса string используется для удаления пробельных символов в начале и конце строки?
1)trim;*
2)strip;
3)erase;
4)remove_whitespace;

№21 Какой метод класса string используется для повторения строки несколько раз?
1)repeat;
2)multiply;
3)duplicate;
4)append;*

№22 Как объединить несколько строк в одну в C++?
1)s1 + s2 + s3;
2)s1.append(s2).append(s3);*
3)s1.concat(s2).concat(s3);
4)s1.add(s2).add(s3);

№23 Как разделить строку на несколько строк по определенному разделителю?
1)split;*
2)slice;
3)substring;
4)subarray;

№24 Как проверить, содержит ли строка определенный символ?
1)s.contains(c);
2)s.find(c) != string::npos;
3)s.at(s.find(c)) == c;
4)все вышеперечисленные;*

№25 Как добавить символ в начало строки в C++?
1)s.insert(0, c);
2)s.prepend(c);
3)s.insert(s.begin(), c);*
4)s.add_first(c);

№26 Как заменить все вхождения одной подстроки на другую в строке?
1)s.replace(str1, str2);
2)s.swap(str1, str2);
3)s.replaceAll(str1, str2);*
4)s.switch(str1, str2);

№27 Как проверить, является ли строка числом?
1)isdigit(s);
2)s.isnumeric();
3)std::isdigit(s[0]);
4)все вышеперечисленные;*
 
№28 Как преобразовать строку в число?
1)stoi(s);
2)atoi(s);
3)std::stoi(s);
4)все вышеперечисленные;*

№29 Как получить последний символ строки?
1)s[s.length() - 1];
2)s.back();
3)s.at(s.length() - 1);
4)все вышеперечисленные;*

№30 Как преобразовать число в строку?
1)to_string(num);
2)std::to_string(num);
3)stringstream s; s << num; string str = s.str();
4)все вышеперечисленные;*